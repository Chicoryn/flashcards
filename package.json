{
  "name": "flashcards-app",
  "version": "0.0.0",
  "scripts": {
    "heroku-prebuild": "cd src/web && npm install --dev",
    "heroku-postbuild": "cd src/web && npm run build",
    "postinstall": "cd src/web && npm install"
  },
  "engines": {
    "node": "10.x"
  }
}
